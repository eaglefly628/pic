# 交付什么 · 怎么算做完

## 一、交付物

1. **改好的代码**，直接改在 `project-one/` 里，能 `npm run dev` 跑起来
2. **一份 `重设计说明.md`**，写清楚：
   - 定了哪套令牌（贴出最终的 CSS 变量表）
   - 24 屏各改了什么（一屏一两句就够）
   - 「01-设计任务书」第五节那 8 条不一致，**哪几条做了、哪几条没做、没做的原因**
   - 有意保留的现状（比如"搜索框仍然是假的，因为 XX"）
3. **改后的 24 屏截图**，跟 `screenshots/` 里同名同角度，方便逐张对照

> 截图脚本可以直接抄包里没附的那份思路：Playwright，viewport `1440×950`，`deviceScaleFactor: 2`，
> 走一遍主密码创建 → 逐页导航 → 弹窗 → 切深色 → 连点 5 次版本号进隐藏区。
> 如果需要原脚本，找项目主人要 `shots.cjs`。

---

## 二、硬性验收（这几条不过就是没做完）

- [ ] `npm run build` 通过，没有新增 TypeScript 报错
- [ ] **24 屏 × 2 主题 = 48 个状态**全部检查过，没有浅底浅字 / 深底深字 / 对比度不足
- [ ] 窗口仍然是固定 1200×800，没有横向滚动条，没有内容溢出被裁
- [ ] 没有引入任何新的运行时依赖（`package.json` 的 `dependencies` 不许变长）
- [ ] 没有任何外部网络请求（CDN / 字体 / 图标），除了财经行情页原有的取数
- [ ] 所有金额数字仍然等宽对齐（`tabular-nums`），负号仍然是 `−`（U+2212）
- [ ] 键盘能走完全部主要流程，`:focus-visible` 描边环还在
- [ ] `prefers-reduced-motion: reduce` 下所有新增动效都关得掉
- [ ] 主密码 / 加密 / 数据结构 / Excel 解析 一行没动

---

## 三、逐屏自查（改完每一屏问自己）

- [ ] 这屏用的字号 / 圆角 / 间距，是不是都来自那套新令牌？还有没有魔法数字漏网？
- [ ] 深色态单独看过没有？（不是"应该没问题"，是真的切过去看过）
- [ ] 空态设计了没有？零数据时长什么样？
- [ ] 数据爆量时长什么样？（50 个账户、200 条收入记录、超长账户名）
- [ ] 破坏性操作有二次确认没有？

---

## 四、重点复查这几处（现存问题最集中）

| 位置 | 检查什么 |
|---|---|
| `09-导入导出` | 导入 Excel 替换全部数据，**必须**加二次确认，措辞要明确说"会覆盖现有全部账户和快照" |
| `12-预算与预测` 的开销列表 | 加上表头，跟 `10-收入情况` 的收入表对齐成同一套 |
| `03/04/05-资金账户` | 补空态；账户表加表头 |
| `06-账户详情` 的余额快照表 | 补空态 |
| `08-利息预测` 的合计行 | 颜色规则跟数据行对齐（数据行零值灰色，合计行也要） |
| `14-密码保险箱` | 修分类列塌陷（没填分类时整列不渲染，行对不齐）；删除加确认 |
| `20-24 独立管理` | 并进主设计系统，只靠一条标识或配色浓度区分 |
| `02-财经行情` | 正常态 / 加载态 / 失败态三个都要设计，失败态别再是一句干巴巴的话占满整页 |
| 顶部搜索框 | 要么做成真能用，要么撤掉。别留着骗人 |
| `compute.ts:354` | `来源` 列恒为「导入」—— 三元的两个分支写成了一样，顺手修掉 |

---

## 五、别做的事

- 别引 Tailwind / MUI / Ant Design / shadcn —— 这是本地个人项目，不背依赖
- 别做响应式断点 —— 窗口固定 1200×800
- 别改中文文案的措辞，除非是为了修一致性（比如统一按钮上的动词）
- 别把 inline style 全量搬进 CSS module / styled-components —— **收敛成令牌就够了**，
  重构样式方案不是这次的目标，会把 diff 撑到没法审
- 别动 `project-one/` 以外的目录
